# Welcome tour

Welcome to the `soc-ops` development workspace — a small React + Vite demo.

- **Start dev server:** `npm run dev` (already running)
- **Run tests:** `npm run test` (passes)
- **Run linter:** `npm run lint` (see notes below)

Notes:
- ESLint currently errors due to `parserOptions.project` and TypeScript project path. This can be fixed by adjusting ESLint settings or `tsconfig`.

Enjoy exploring the code in `src/`!
